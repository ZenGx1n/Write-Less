<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.4//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.writeless.dao.UserDAO">

    <resultMap id="user" type="User">
        <id property="id" column="id" javaType="Integer"/>
        <result property="username" column="username" javaType="String"/>
        <result property="password" column="password" javaType="String"/>
        <result property="gender" column="gender" javaType="String"/>
        <result property="createTime" column="create_time" javaType="java.util.Date"/>
        <result property="age" column="age" javaType="Integer"/>
        <result property="post" column="post" javaType="String"/>
        <result property="permission" column="permission" javaType="Integer"/>
    </resultMap>

    <insert id="insert" parameterType="User">
        INSERT INTO user (username,password,gender,create_time,age,post)
                    VALUES
                         (#{username},#{password},#{gender},#{createTime},#{age},#{post})
    </insert>

    <update id="update" parameterType="User">
        UPDATE user SET username=#{username},password=#{password},gender=#{gender},
        age=#{age},post=#{post}
    </update>

    <select id="delete" parameterType="Integer">
        DELETE FROM user WHERE id=#{id}
    </select>

    <select id="selectById" parameterType="Integer" resultMap="user">
        SELECT * FROM user where id=#{id}
    </select>

    <select id="selectAll" resultMap="user">
        SELECT * FROM user;
    </select>

</mapper>
